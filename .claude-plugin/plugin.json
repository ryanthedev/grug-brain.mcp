{
  "name": "grug-brain",
  "version": "2.0.0",
  "description": "Persistent memory for LLMs with search, docs, and dreaming. SQLite FTS5, markdown storage, git-tracked history.",
  "mcpServers": {
    "grug-brain": {
      "command": "bun",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server.js"]
    }
  }
}
