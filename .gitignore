# SQLite databases (live at ~/.grug-brain/, not in repo)
*.db
*.db-wal
*.db-shm

# Runtime files
memories/recall.md

# Rust build artifacts
target/

# Ingested docs (managed via brains.json, not committed)
docs/
